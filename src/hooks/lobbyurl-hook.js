import {
  // useState,
  // useRef,
  // useEffect,
  useContext,
  // useCallback
} from 'react';
import { UserContext } from '../context/contexts';

export const useLobbyURL = () => {

  const { userId } = useContext(UserContext);


  return {

  };
}